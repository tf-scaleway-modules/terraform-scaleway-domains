terraform {
     required_providers {
         scaleway = {
            source = "scaleway/scaleway"
            version = "2.53.0" 
            } 
          }
        }

provider "scaleway" { 
    # Configuration options 
}